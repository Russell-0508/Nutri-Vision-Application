import React from 'react';
import { 
    View, 
    ImageBackground, 
    StyleSheet, 
    Image, 
    Text, 
    Dimensions, 
    Button,
    TouchableOpacity,
    SafeAreaView,
    ScrollView,
    TextInput,
} from 'react-native';

import { useNavigation } from "@react-navigation/native"

function IndiMeal({navigation}) {
    return (
        <View> 
            <Text> Individual Meal page</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    
})

export default IndiMeal;